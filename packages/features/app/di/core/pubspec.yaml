name: di_core
description: "App DI core — shared infrastructure modules (storage, HTTP)."
publish_to: none
version: 1.0.0
resolution: workspace

environment:
  sdk: 3.11.1
  flutter: 3.41.4

dependencies:
  core:
  database:
  flutter:
    sdk: flutter
  network:
  storage:
  yx_scope: 1.0.2
