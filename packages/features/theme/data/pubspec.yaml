name: theme_data
description: "Theme feature data layer - repositories and services."
publish_to: none
version: 1.0.0
resolution: workspace

environment:
  sdk: 3.11.0
  flutter: 3.41.0

dependencies:
  theme_domain:
  app_domain:
  storage:
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: 2.5.4
