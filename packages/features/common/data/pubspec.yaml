name: common_data
description: "Common feature data layer - repositories and services."
publish_to: none
version: 1.0.0
resolution: workspace

environment:
  sdk: 3.11.0
  flutter: 3.41.0

dependencies:
  app_data:
  common_domain:
  database:
  flutter:
    sdk: flutter
  meta: ^1.15.0
  network:

dev_dependencies:
  build_runner: 2.5.4
