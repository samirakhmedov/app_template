name: splash_data
description: "Splash feature data layer - repositories and services."
publish_to: none
version: 1.0.0
resolution: workspace

environment:
  sdk: 3.11.0
  flutter: 3.41.0

dependencies:
  splash_domain:
  api:
  database:
  network:
  app_domain:
  app_data:

dev_dependencies:
  build_runner: 2.5.4
