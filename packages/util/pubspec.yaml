name: util
description: "Utility package with extensions, fields, and helpers."
publish_to: none
version: 1.0.0
resolution: workspace

environment:
  sdk: 3.11.1
  flutter: 3.41.4

dependencies:
  app_assets:
  flutter:
    sdk: flutter
