name: app_template
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: 3.8.1
  flutter: ">=3.3.0"

dependencies:
  # analytics_interface:
  #   path: packages/analytics/interface
  # analytic:
  #   path: packages/analytics/gms
  # push_interface:
  #   path: packages/push/interface
  # push:
  #   path: packages/push/gms
  # location_interface:
  #   path: packages/location/interface
  # location:
  #   path: packages/location/gms
  api:
    path: packages/api
  auto_route: 10.1.0+1
  bloc: 9.0.0
  bloc_concurrency: 0.3.0
  collection: 1.19.1
  database:
    path: packages/database
  flutter:
    sdk: flutter
  flutter_bloc: 9.1.1
  flutter_easy_dialogs: 4.0.3
  flutter_localizations:
    sdk: flutter
  freezed_annotation: 3.0.0
  haptics:
    path: packages/haptics
  intl: 0.20.2
  network:
    path: packages/network
  provider: 6.1.5
  storage:
    path: packages/storage
  uikit:
    path: packages/uikit
  vector_graphics: 1.1.19
  yx_scope: 1.0.2

dev_dependencies:
  auto_route_generator: 10.2.3
  build_runner: 2.4.15
  flutter_test:
    sdk: flutter
  freezed: 3.0.6
  mocktail: 1.0.4
  surf_dcm_presets: 0.1.1
  surf_lint_rules: 4.0.0
  surf_widget_test_composer: 0.0.6
  vector_graphics_compiler: 1.1.17

flutter:
  generate: true
  default-flavor: prodGms
  uses-material-design: false

  assets:
    - assets/resources/vectors/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700

# TODO: introduce accessibility package
# TODO: Introduce HTTP client factory for each scope
# TODO: move snack queue bloc to app scope
# TODO: test haptic on iOS
# TODO: include fragment shaders into project (f.e., shimmers, gradient)
# TODO: introduce asset transformation at build time - https://docs.flutter.dev/ui/assets/asset-transformation
# TODO: add DTO utils to API package 

flutter_gen:
  output: lib/generated/
  line_length: 100
