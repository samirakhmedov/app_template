/// {@template k_default_url}
/// The default URL for the API.
/// {@endtemplate}
const kDefaultUrl = 'https://api.example.com';

/// {@template k_default_connect_timeout}
/// The default connect timeout for the API.
/// {@endtemplate}
const kDefaultConnectTimeout = Duration(seconds: 30);

/// {@template k_default_receive_timeout}
/// The default receive timeout for the API.
/// {@endtemplate}
const kDefaultReceiveTimeout = Duration(seconds: 30);

/// {@template k_default_send_timeout}
/// The default send timeout for the API.
/// {@endtemplate}
const kDefaultSendTimeout = Duration(seconds: 30);

/// {@template k_default_animation_duration}
/// The default animation duration.
/// {@endtemplate}
const kDefaultAnimationDuration = Duration(milliseconds: 300);
